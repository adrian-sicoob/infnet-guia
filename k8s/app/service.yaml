apiVersion: v1
kind: Service
metadata:
  name: infnet-guia-service
  namespace: infnet-guia
  labels:
    app: infnet-guia
spec:
  type: NodePort
  selector:
    app: infnet-guia
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
      nodePort: 30000
